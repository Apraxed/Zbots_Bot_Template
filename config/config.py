import os
# from dotenv import dotenv

# Token(replit users i reccomend using the environ secrets)
os.environ['TOKEN'] # for .env files and environ variable secrets
TOKEN: str = "INSERT_TOKEN_HERE"

# Prefix
PREFIX = "!"

EMBED_COLOR = 0x433bdb  #replace after'0x' with desired hex code ex. '#ff0188' >> '0xff0188'

NoChannelsPassword = 'deleteall' # Password for the owner only command that deletes the channels